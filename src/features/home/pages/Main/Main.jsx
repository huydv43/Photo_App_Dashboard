import React from 'react';
import PropTypes from 'prop-types';

Main.propTypes = {
    
};

function Main(props) {
    return (
        <div>
            this is home page
        </div>
    );
}

export default Main;