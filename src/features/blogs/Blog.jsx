import React from 'react';
import PropTypes from 'prop-types';

Blog.propTypes = {
    
};

function Blog(props) {
    return (
        <div>
            this is blog index
        </div>
    );
}

export default Blog;